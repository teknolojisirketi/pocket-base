services:
  pocketbase:
    build: .
    volumes:
      - ../pb_data:/pb/pb_data
      - ../pb_hooks:/pb/pb_hooks
      - ../pb_migrations:/pb/pb_migrations
    ports:
      - "8080:8090"
    restart: unless-stopped
